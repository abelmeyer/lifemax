// Head-slot art. Anchor to headCenter(metrics) from ./shared.

// TODO(art): Sweat Headband — placeholder, renders nothing until drawn.
export function SweatHeadband() {
  return null;
}

// TODO(art): Knit Beanie — placeholder, renders nothing until drawn.
export function KnitBeanie() {
  return null;
}

// TODO(art): Snapback Cap — placeholder, renders nothing until drawn.
export function SnapbackCap() {
  return null;
}

// TODO(art): Boxing Headgear — placeholder, renders nothing until drawn.
export function BoxingHeadgear() {
  return null;
}

// TODO(art): Champion's Crown — placeholder, renders nothing until drawn.
export function ChampionsCrown() {
  return null;
}
